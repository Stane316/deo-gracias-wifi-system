import { describe, expect, it } from 'vitest';
import { FRONTEND_PLACEHOLDER } from './index.js';

describe('frontend skeleton', () => {
  it('est en attente d IMP-25', () => {
    expect(FRONTEND_PLACEHOLDER).toBe('imp-25');
  });
});
